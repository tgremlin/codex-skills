# AGENTS

- Test Codex skills in this repo: /mnt/Storage/Repos/codex-skills.
- Use repo-local temp: set `PF_TMP_DIR` or default to `{repo}/.tmp-test` for all skills.
- Gate tests are scoped to `tests/` when present; add `pytest.ini` to broaden/override.
