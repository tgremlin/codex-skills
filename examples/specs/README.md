# Specs Catalog

- `crud_basic.md`: baseline CRUD + persistence checks.
- `crud_auth.md`: CRUD with authentication/authorization response expectations.
- `crud_search_filter.md`: CRUD plus search/filter query behavior.
- `crud_rbac_audit_job.md`: multi-faceted scenario with RBAC, persisted audit logs, scheduled retention job, and role-aware UI expectations.
